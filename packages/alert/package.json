{
  "name": "@liquidius/alert",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo skipped"
  },
  "dependencies": {
    "@liquidius/core": "workspace:*",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.3.0"
  }
}
